rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /protokolle/{protokollId} {
      allow read, write: if request.auth != null;

      match /fotos/{fotoId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
